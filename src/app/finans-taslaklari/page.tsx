import { izinVar, requirePagePermission } from "@/lib/rbac/guard";
import { prisma } from "@/lib/db";
import { FinansTaslaklari } from "@/components/finans-taslaklari";
import { PageBaslik } from "@/components/page-baslik";
export const dynamic="force-dynamic";
export default async function FinansTaslaklariPage(){const actor=await requirePagePermission("FINANS","GORUNTULE");const onayYetkisi=izinVar(actor,"FINANS","ONAYLA");const iptalYetkisi=izinVar(actor,"FINANS","IPTAL");const[taslaklar,hesaplar]=await Promise.all([prisma.finansTaslagi.findMany({where:{firmaId:actor.firmaId,durum:{in:["INCELEME_BEKLIYOR","EKSIK_BILGI"]}},include:{cari:true,sesliNot:{include:{sesKaydi:true}},olusturan:true},orderBy:{createdAt:"asc"}}),prisma.kasaHesap.findMany({where:{firmaId:actor.firmaId,aktif:true},orderBy:{ad:"asc"}})]);return <div className="space-y-4"><PageBaslik baslik="Finans taslakları" alt="Sesli notlardan gelen, inceleme bekleyen finans kayıtları" geri="/finans"/><FinansTaslaklari taslaklar={taslaklar.map(t=>({id:t.id,sesliNotId:t.sesliNotId,cari:t.cari.ad,metin:t.sesliNot.duzeltilmisMetin??t.sesliNot.hamMetin,sesVar:!!t.sesliNot.sesKaydi,createdAt:t.createdAt.toISOString(),hatirlatmaAt:t.hatirlatmaAt.toISOString(),tip:t.onerilenTip,tutar:t.onerilenTutar?Number(t.onerilenTutar):null,paraBirimi:t.onerilenParaBirimi}))} hesaplar={hesaplar.map(h=>({id:h.id,ad:h.ad,paraBirimi:h.bakiyeTuru}))} onayYetkisi={onayYetkisi} iptalYetkisi={iptalYetkisi}/></div>}
