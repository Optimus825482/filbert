# Taste
- Communicates in Turkish and expects work products (code, comments, commit/summary text, explanations) in Turkish. Confidence: 0.8
- Wants issues fixed end-to-end in a single autonomous pass: don't stop to ask which findings to prioritize — resolve everything reported, and only report back when done ("TÜMÜNÜ DÜZELT, BİTİRENE KADAR DURMA"). Confidence: 0.85
- Expects complete, error-free results ("eksiksiz hatasız"): validate with typecheck, lint, tests, and a production build before declaring the work finished — in this repo the gate is `pnpm check` (lint + typecheck + unit tests) followed by `pnpm build`. Confidence: 0.7
- Deploys via Coolify on a self-hosted server: code is pushed to GitHub first, then Coolify builds/deploys it and it is served on a custom (sub)domain under erkanerdem.online. Confidence: 0.6
- Prefers deploying with a single docker-compose.yaml: the app and its database run as two separate containers within the same project (nothing deployed piecemeal/separately), and the very first startup must create the database, tables, indexes and constraints automatically (e.g. via migrations-run-as-a-compose-step) rather than requiring manual SQL setup. Confidence: 0.6
- Ships work by committing and pushing straight to the GitHub `main` branch (not `master`), and expects the whole change set committed in one go with the remote/upstream verified afterward. Confidence: 0.6
- Reports bugs by pasting raw terminal/dev-server/deployment logs verbatim (request traces, full stack traces, Prisma error dumps, full Coolify build/deploy output with Docker layer timings) and expects root-cause diagnosis plus a real fix — not just an explanation of the error. Confidence: 0.65
- For bugs that only appear in production, reproduces the production topology locally before calling it fixed: a throwaway reverse proxy that mimics the platform's edge (Coolify/Traefik, injecting `x-forwarded-proto`) plus a real browser (Playwright/Chromium) to observe the actual cookies/headers, instead of concluding from reading source or from a localhost-only test. Confidence: 0.55
- Prefers the minimal-change solution: when the root cause is an environment/panel misconfiguration (e.g. a domain entered without a scheme in Coolify), expects the config fix alone to be enough and does not want extra speculative code changes layered on top. Confidence: 0.5
- A fix is only "verified" when exercised against the real runtime/database, not just mocked or skipped tests: guardrail tests that only regex-inspect source files aren't enough, and verification is expected to run against a real DB (rollback-isolated so no dev data is mutated); UI work likewise renders the real page against throwaway seed data that is deleted afterwards, with the workspace (temp scripts/screenshots, ports) left clean. Confidence: 0.6
- Filbert models hazelnut as a single variety: the çeşit/cins distinction (Giresun/Levant/Ordu/Diğer) is not wanted anywhere — no variety enum, selector, column, filter or display label. Confidence: 0.7
- When a concept is retired, expects it removed exhaustively across the whole system, not just the surface named: Prisma schema + migration, server actions, all forms, list/detail/receipt screens, shared label dictionaries, tests and docs ("tüm formlarda ve sistemde ... kaldır"). Confidence: 0.6
- Works on several separate applications in parallel and corrects the agent when a reported symptom belongs to a different app; scope should be confirmed against the current project before code is changed ("yanlış alarm, farklı bir uygulamadaki sorundu"). Confidence: 0.5
- Keeps the dashboard strictly non-financial: the main page (Pano) may show only physical/operational figures (kg, adet/fiş adedi, randıman puanı, stok, emanet kg) and must contain no money-related statistics at all (Tutar/TL/₺, ödeme/alım tutarı); monetary figures belong only on finance/report screens (Kasa, Banka, Finans/Ödeme, raporlar). Confidence: 0.8
- Wants the main page (Pano / ana sayfa) designed as a real desktop dashboard, not a vertical stack of KPI cards + buttons + one table: a multi-column grid that fits a single screen, metric strip with hairline separators, a trend chart, a pending-work/alert rail, a condensed recent-records table and a distribution panel — while the mobile view is left alone. Confidence: 0.55
- Design/UI work is verified by actually rendering the page in a browser, capturing screenshots and inspecting them in both light and dark themes (checking overlap, clipping, horizontal overflow, dead space, contrast) rather than concluding from a successful build. Confidence: 0.5
- Destructive, production-affecting changes (e.g. a migration that drops columns and cannot restore the data, whose push triggers a Coolify deploy) should be committed locally and held for explicit approval rather than pushed straight to `main`. Confidence: 0.5
- Prefers desktop sidebar navigation grouped into collapsible sections: clicking a group heading expands/collapses its items downward, so long menus scan better and need less scrolling — wants such changes to improve usability, not just looks. Confidence: 0.5
